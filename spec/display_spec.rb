# display_spec.rb
require './lib/display'

describe Display do
  subject(:display) { Display.new }

  describe '#prompt_for_name' do
    # Only puts and gets, doesn't need to be tested
  end

  describe '#prompt_for_play' do
    # Only puts and gets, doesn't need to be tested
  end
  
  describe '#print_board' do
    # Only prints info to screen, doesn't need to be tested
end
